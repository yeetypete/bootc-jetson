# Load the NVIDIA GPU and display stack at boot, since bootc masks
# nv-load-display-modules.service (it depmods read-only /usr).
# nvgpu must load first to claim the GPU before nvidia.ko. nvidia_drm pulls in
# nvidia_modeset and nvidia via modules.dep.
nvgpu
nvidia_drm
