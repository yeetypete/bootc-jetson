# nvfancontrol stores state under /var/lib/nvfancontrol, but the
# nvidia-l4t-nvfancontrol package ships neither the directory nor a
# StateDirectory= in its unit. On bootc /var starts empty at first boot,
# so create the directory here.
d /var/lib/nvfancontrol 0755 root root -
