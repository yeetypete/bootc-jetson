# nvfancontrol stores state under /var/lib/nvfancontrol but ships no directory or
# StateDirectory=, and bootc /var starts empty at first boot, so create it here.
d /var/lib/nvfancontrol 0755 root root -
