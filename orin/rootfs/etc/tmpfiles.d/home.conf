# Masks the stock /usr/lib/tmpfiles.d/home.conf. Here /home -> /var/home and
# /srv -> /var/srv are image symlinks provisioned by bootc-base-dirs.conf. The
# stock "Q /home" / "q /srv" entries try to recreate them as directories and fail
# at boot. Intentionally empty.
