# Thor (gb10b GPU) uses openrm (nvidia.ko). nvgpu is the Orin driver and must not
# load. The GPU's PCI modalias also matches nvgpu/nouveau, so udev autoloads them
# by modalias and nvgpu binds the GPU before openrm can. Blacklist them.
blacklist nvgpu
blacklist nouveau
