# The GPU's PCI modalias matches nvidia and nvidia_drm, so udev autoloads them
# the moment the GPU enumerates, before the platform is ready, and the probe
# fails. Suppress the modalias autoload here. nvidia-display-load.service loads
# the stack once the platform is up.
blacklist nvidia
blacklist nvidia_drm
